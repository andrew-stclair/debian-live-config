{
    // See https://go.microsoft.com/fwlink/?LinkBId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build ISO",
            "type": "shell",
            "command": "make install_buildenv && make",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}